{
  "name": "deeprank-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static DeepRank website with Netlify contact lead functions.",
  "scripts": {
    "check": "node --check script.js && node --check admin/contact-leads.js && node --check netlify/functions/contact-leads.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
